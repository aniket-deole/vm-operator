# Tasks: Tag CRD + Tag Controller for Affinity

- **Spec**: [`spec.md`](./spec.md)
- **Plan**: [`plan.md`](./plan.md)
- **Model**: [`model.md`](./model.md)
- **E2E plan**: [`e2e.md`](./e2e.md)
- **Epic**: vmop-3882

<!--
The [vmop-NNN] tags below are allocated from vmop-11000 upward, in task order. Each one must exist as a story or sub-task and be linked to epic vmop-3882 via customfield_10830 (Epic Link), set post-create via PUT — the JIRA create screen does not expose it. Check off each task as it lands, and add any task discovered mid-implementation.

Tasks discovered after the initial numbering keep their position in the ordered list and take a lettered suffix (T011a, T011b, …) rather than renumbering everything downstream; their tickets are allocated from the next free number, so ticket order and task order diverge from that point on. That is expected.
-->

## Phase 1 — Setup

- [ ] T001 [vmop-11000] Add `Tag`, `TagList`, `TagSpec`, `TagStatus`, the `Ready` reason constants, and the printer-column / root / storageversion / status-subresource markers in `external/vsphere-policy/api/v1alpha1/tag_types.go`, appending both types to `objectTypes` from `init()` — schema and markers per [`model.md`](./model.md) "Schema"
- [ ] T002 [vmop-11001] Regenerate deepcopy and the external CRD manifest (`make generate-go generate-external-manifests`) — `external/vsphere-policy/api/v1alpha1/zz_generated.deepcopy.go`, `config/crd/external-crds/vsphere.policy.vmware.com_tags.yaml`. No Makefile change is needed (the target already globs the module), no `config/crd/crd.go` change is needed (its embed directive globs `vsphere.policy.vmware.com_*.yaml`), and no envtest change is needed (`test/builder/test_suite.go` loads the whole `config/crd/external-crds` directory)
- [ ] T002a [vmop-11040] Install the CRD on a Supervisor — see [`plan.md`](./plan.md) "Getting the CRD onto a Supervisor": add the manifest to the install list in `config/default/kustomization.yaml` beside the three existing `vsphere.policy` entries, and list it under "Production CRDs" in `config/crd/external-crds/README.md`
- [ ] T002b [vmop-11041] Add `case "Tag":` to the `switch` in `pkg/crd/crd.go`'s `Install`, gated on `features.TaggingAPI`, so the CRD is created when the flag is on and **deleted** when it is off — matching how its `ComputePolicy` / `PolicyEvaluation` / `TagPolicy` siblings are gated on `features.VSpherePolicies`. Without this the kind falls through to `default:` and is installed unconditionally, which both leaves unserviceable API surface on a flag-off Supervisor and breaks the E2E suite's gating signal ([`e2e.md`](./e2e.md) "Gating")
- [ ] T003 [P] [vmop-11002] Add `Features.TaggingAPI` to `pkg/config/config.go` and seed it `true` in `pkg/config/default.go` for this development branch (no capability mapping — spec NG6)
- [ ] T004 [P] Register `&vspherepolv1.Tag{}` in `KnownObjectTypes` in `test/builder/fake.go` so the fake client enforces the status-subresource split

## Phase 2 — Foundational

- [ ] T005 [vmop-11003] Declare the four field-index keys and their extractor functions in `pkg/util/kube/affinitytag.go`, following the `VMSpecVolumesPVCsIndexKey`/`VMSpecVolumesPVCsIndexerFunc` convention in `pkg/util/kube/pvc.go`: `spec.label`, `spec.labelKeyValue`, and `metadata.ownerReferences.uid` on `vspherepolv1.Tag`, plus the multi-valued `metadata.labels.keyValue` on `vmopv1.VirtualMachine` (one `"<key>:<value>"` per label after `kubeutil.RemoveVMOperatorLabels`; the extractor MUST NOT mutate the cached object). Expose all four through one `RegisterAffinityTagIndexes(ctx, fieldIndexer) error` helper — it is called from exactly one place (T016) because registering the same object/field twice is an error, and the VM controller's mapper (T016a) depends on that single registration
- [ ] T005a [vmop-11048] Add `TagToVirtualMachineMapper(ctx, k8sClient) handler.MapFunc` and `TagFanOutPredicate() predicate.Predicate` to the same file. The mapper does one **indexed** `List` (`client.InNamespace` + `client.MatchingFields{VMLabelKeyValueIndexKey}`) and returns one request per hit — not `client.MatchingLabels`, which filters in memory after listing the namespace (`operator-best-practices.md` "Mapper Functions"). The predicate returns true on create, on delete, and on the update that sets `deletionTimestamp`, and **false** for every other update, so the `Tag` controller's own status/finalizer/label-mirror writes and the VM path's owner-reference patches do not wake every VM carrying the label. This file, not `pkg/util/vmopv1` where the other VM mappers live: the mapper needs `VMLabelKeyValueIndexKey`, and the edge `pkg/util/kube → pkg/util/vmopv1` already exists (`vmsnapshot.go`) so it cannot be reversed. See [`plan.md`](./plan.md) "Fan-out — the VM controller's `Tag` watch" and D19
- [ ] T006 [P] [vmop-11004] Unit tests for the extractors in `pkg/util/kube/affinitytag_test.go`: multi-label VMs, reserved-label filtering, empty values, prefixed keys, a VM with no labels, and a `Tag` with several owner references
- [ ] T006a [P] [vmop-11049] Unit tests in the same file for T005a: the mapper returns one request per label-carrying VM and none for same-key-different-value VMs, VMs in other namespaces, or a `Tag` nothing matches; the predicate admits create, delete and the `deletionTimestamp` transition, and rejects status-only, finalizer-only, label-only and owner-reference-only updates
- [ ] T007 [vmop-11005] Implement the shared helpers `AffinityLabelPairs`, `TagResourceName`, `VCenterTagName` and the `ExtraConfigAffinityTagsKey` constant in a **new file beside the existing affinity logic**, `pkg/providers/vsphere/virtualmachine/affinitytag.go`; refactor `extractAffinityLabelsFromVM` in `affinity.go` to call `AffinityLabelPairs` so the flag-on and flag-off paths cannot diverge on what "referenced by affinity" means. `AffinityRuleConstraints` and `CalculateAffinityConstraints` MUST NOT move, be aliased, or change signature — all 31 existing references stay untouched (spec SC-006). Not `pkg/util/vmopv1`: `pkg/util/kube` already imports it, so it cannot import `pkg/util/kube` back for `RemoveVMOperatorLabels` / `HasCAPILabels` / `HasZoneLabel` — see [`plan.md`](./plan.md) "Why this package, and the one rule it imposes"
- [ ] T008 [P] [vmop-11006] Unit tests for the shared helpers (selector operators, topology-key eligibility per `AffinityRuleConstraints`, prefixed keys, empty values, name determinism) in a new `pkg/providers/vsphere/virtualmachine/affinitytag_test.go` — a new file, not additions to `affinity_test.go`, so the pre-existing suite stays the untouched flag-off baseline
- [ ] T009 [vmop-11007] Gate mechanism A off when the flag is on, at both `genConfigSpecTagSpecsFromVMLabels` call sites in `pkg/providers/vsphere/virtualmachine/configspec.go` (`CreateConfigSpec`, `CreateConfigSpecForPlacement`); leave `genConfigSpecAffinityPolicies` untouched — see [`plan.md`](./plan.md) "Every emission point is gated"
- [ ] T009a [vmop-11044] Implement the read-only `AppendExistingTagSpecs(ctx, k8sClient, vmCtx, configSpec, recordApplied)` in `pkg/providers/vsphere/virtualmachine/affinitytag.go`: for each of the VM's own labels, `Get` the `Tag` by derived name and, if present and not terminating, append an add-only `NameId` `TagSpec`. It MUST NOT create a `Tag`, write an owner reference, requeue on a terminating `Tag`, or error on a spec mismatch — all of that belongs to `ReconcileTagCRs` (D2, D17). `recordApplied` additionally appends the `ExtraConfigAffinityTagsKey` record for the emitted set
- [ ] T009b [vmop-11045] Call it at the flag-on emission points: `recordApplied=false` at the two placement sites (`pkg/providers/vsphere/vmprovider_vm.go` ~1786, `pkg/providers/vsphere/vmprovider_vmgroup.go` ~245) and `recordApplied=true` at the create site (`vmprovider_vm.go` ~2581, `createArgs`). Add **nothing** at the `updateArgs` (~3274) and `resizeArgs` (~3340) sites — those ConfigSpecs reach `doReconfigure`, where `ReconcileTagSpecs` already emits the full diff, so emitting here too would add the same tag twice to one slice
- [ ] T010 [P] [vmop-11008] Extend the existing affinity/configspec unit tests so the flag-off path is asserted byte-for-byte unchanged at all four emission points, and the flag-on path emits no mechanism-A tag specs (spec SC-006) — `pkg/providers/vsphere/virtualmachine/affinity_test.go`, `pkg/providers/vsphere/virtualmachine/configspec_test.go`
- [ ] T010a [P] [vmop-11046] Unit tests for `AppendExistingTagSpecs` in `pkg/providers/vsphere/virtualmachine/affinitytag_test.go`: emits for an existing `Tag` matching one of the VM's labels; emits nothing when no `Tag` exists (the first-VM-in-a-relationship case), when the `Tag` is terminating, or when the same key carries a different value; creates no `Tag` and writes no owner reference in any case; `recordApplied=true` appends the ExtraConfig record and `false` does not

## Phase 3 — User Story 1 (DevOps user: affinity-referenced label becomes a vCenter tag)

- [ ] T011 [US1] [vmop-11009] Implement `ReconcileTagCRs` (steps 1-3 of [`plan.md`](./plan.md) "`ReconcileTagCRs`") in `pkg/vmconfig/affinitytag/affinitytag_reconciler.go`: compute owned pairs (labels ∩ affinity), ensure a `Tag` exists per owned pair, and add/remove **only this VM's** ownerReference under `MergeFromWithOptimisticLock` with a skip-if-unchanged guard. MUST return the owned `Tag` objects it ensured — T011a needs them and cannot re-read them (see T011a)
- [ ] T011a [US1] [vmop-11034] Implement `ReconcileTagSpecs` (steps 4-5) in the same file: desired-tag-set computation as the **union** of the per-label-key indexed `List` and the owned `Tag`s handed over by T011, then the `TagSpec` add/remove diff against this feature's own ExtraConfig record. The union is a correctness requirement — the provider's client is cached, so this pass's `Create` is not readable by this pass's `List` ([`research.md`](./research.md) "The provider's Kubernetes client is cached"). The tag diff is driven by labels ∩ existing `Tag`s and MUST NOT consult the VM's own `spec.affinity`
- [ ] T011b [US1] [vmop-11035] Implement the ExtraConfig record workflow in `ReconcileTagSpecs` against the `ExtraConfigAffinityTagsKey` declared in T007, mirroring `pkg/vmconfig/policy`: adds are `shouldBe \ haveBeen`, removes are `haveBeen \ shouldBe`, and the comma-joined record is appended to `configSpec.ExtraConfig` **only when it differs** so an unchanged reconcile writes nothing. `pkgctx.GetVMTags` MUST NOT be used — it returns tag URNs that cannot be matched to name+category tags (D12)
- [ ] T012 [US1] [vmop-11010] Add the two call sites to `doReconfigure` in `pkg/providers/vsphere/session/session_vm_update.go`, both behind `Features.TaggingAPI`, in the order `ReconcileTagCRs` → `reconcileVSpherePolicies` → `ReconcileTagSpecs`. **This is what dispatches the reconciler** — `vmconfig.Register` drives only `OnResult`, behind a BYOK/Telco gate ([`plan.md`](./plan.md) "How it is dispatched"). Both orderings are load-bearing; do not collapse the two call sites into one
- [ ] T012a [US1] [vmop-11036] Register `affinitytag.New()` behind `Features.TaggingAPI` in `controllers/virtualmachine/virtualmachine/virtualmachine_controller.go` for interface conformity (its `Reconcile` calls both halves in order, its `OnResult` is a no-op, and neither is on the production path), and add the `vsphere.policy.vmware.com/tags` RBAC marker there — `get;list;watch;create;update;patch;delete`, the `watch` verb being what T016a's watch needs
- [ ] T013 [US1] [vmop-11011] Export `ReleaseOwnership(ctx, k8sClient, vm)` from `pkg/vmconfig/affinitytag` (the same index-driven removal branch `ReconcileTagCRs` uses, so all owner-reference writes stay in one package) and call it from **both** provider delete methods in `pkg/providers/vsphere/vmprovider_vm.go` — `CleanupVirtualMachine` (~326, skip-delete path) and `DeleteVirtualMachine` (~369, normal path). Cycle-safe: `pkg/providers/vsphere` already imports eight `pkg/vmconfig/*` packages, and D13's rule constrains only the child package `pkg/providers/vsphere/virtualmachine`. `ReconcileDelete` picks exactly one of the two, so both must do it or that branch strands an owner reference. Place the call **before** the vCenter work: `DeleteVirtualMachine` returns early on `ErrReconcileInProgress`, a client failure, a `getVM` failure, and `vcVM == nil` — the last returns `nil`, so the controller removes the finalizer and the `Tag` keeps a dangling owner reference for a VM that never reached vCenter. Errors must propagate so finalizer removal is blocked. Do **not** touch the `skipDelete` (pause / `CheckAnnotationDelete`) branch — it keeps the finalizer, so the VM is still a legitimate owner. See [`plan.md`](./plan.md) "Releasing ownership when the VM is deleted"
- [ ] T013b [P] [US1] [vmop-11047] Unit tests for T013 in `pkg/providers/vsphere/` : ownership released on the normal delete path, on the skip-delete path, and for a VM with no vCenter VM (`vcVM == nil`); an error from the release propagates; the `skipDelete` branch releases nothing
- [ ] T013a [US1] [vmop-11037] Extend `removeTagAssociations` in `pkg/providers/vsphere/virtualmachine/cleanup.go` to emit a `NameId` removal per entry recorded in `ExtraConfigAffinityTagsKey` — same package, so no import — when `Features.TaggingAPI` is on, so a VM detached via the skip-delete-platform-resource path does not keep this feature's tags — see [`plan.md`](./plan.md) "Platform-resource cleanup". No ExtraConfig-key cleanup is needed: `removeVMOperatorExtraConfig` already blanks every `vmservice.`-prefixed key
- [ ] T014 [P] [US1] [vmop-11012] Unit tests for the reconciler in `pkg/vmconfig/affinitytag/affinitytag_reconciler_test.go` + suite bootstrap `affinitytag_reconciler_suite_test.go`: `Tag` create shape, second owner appended not replaced, owner removed on affinity change and on VM delete, mismatched-spec collision error, no-op reconcile writes nothing, step 4 issuing one indexed `List` per distinct label key rather than an unfiltered namespace list, and — per spec "Ownership vs. tag carriage" — a pair referenced but not carried creates no `Tag`, while a VM that stops referencing a label it still carries keeps the tag as long as another owner remains
- [ ] T014a [P] [US1] [vmop-11038] Unit tests in the same file for the two mechanisms T011a/T011b add: a `Tag` created in this pass is in the desired set **even when the client's `List` does not return it** (force this, so a regression to a `List`-only desired set fails deterministically instead of racing the cache); and the ExtraConfig record — written on first apply, not rewritten when the set is unchanged, rewritten alongside `TagSpec{remove}` when a pair drops out, adds-only when no record exists yet
- [ ] T014b [P] [US1] [vmop-11039] Unit tests for T013a in `pkg/providers/vsphere/virtualmachine/cleanup_test.go`: recorded affinity tags are removed with the flag on, mechanism B's UUID removals still emitted when both flags are on, nothing emitted for this feature with the flag off
- [ ] T015 [US1] [vmop-11013] Integration test (vcsim) in `pkg/providers/vsphere/session/session_vm_update_test.go` that the emitted `TagSpec`s reach the reconfigure call, the ExtraConfig record round-trips, and — with both `TaggingAPI` and `VSpherePolicies` on — the resulting `configSpec.TagSpecs` carries mechanism B's UUID entries unmodified alongside this feature's `NameId` entries, which is what pins the T012 ordering

## Phase 4 — User Story 2 (label-only VM is tagged when a later VM references the label)

- [ ] T016 [US2] [vmop-11014] Implement the `Tag` controller in `controllers/vspherepolicy/tag/tag_controller.go`: canonical reconcile loop, `vmoperator.vmware.com/tag` finalizer, label-mirror correction, `status.observedGeneration` + `Ready`. Call `kubeutil.RegisterAffinityTagIndexes` (T005) once from `AddToManager` — the VM controller's mapper depends on it and it MUST NOT be registered again there. Add the `tags` and `tags/status` RBAC markers. The controller MUST NOT enqueue VMs on any path: the fan-out is the watch in T016a (D16, D19), so no `virtualmachines` marker and no `List` of VMs belong here
- [ ] T016a [US2] [vmop-11050] Add the fan-out watch to `controllers/virtualmachine/virtualmachine/virtualmachine_controller.go`'s `AddToManager`, behind `Features.TaggingAPI` and beside the existing `EncryptionClass` / `VirtualMachineImageCache` watches: `Watches(&vspherepolv1.Tag{}, handler.EnqueueRequestsFromMapFunc(kubeutil.TagToVirtualMachineMapper(ctx, r.Client)), ctrlbuilder.WithPredicates(kubeutil.TagFanOutPredicate()))`. This is what reconciles owners and label-only VMs alike when a `Tag` appears or starts terminating (spec G2, US2). The `tags` RBAC marker T012a adds already covers the required `watch` verb
- [ ] T017 [US2] [vmop-11015] Register the `Tag` controller behind the flag in `controllers/vspherepolicy/controllers.go`
- [ ] T018 [US2] [vmop-11016] Implement `ReconcileDelete`: remove the finalizer, and nothing else. The VMs were already enqueued by the watch when `ReconcileNormal`'s zero-owner `Delete` set `deletionTimestamp`, and the object's disappearance produces a second watch event that catches any VM whose reconcile raced ahead of that update (D16). Keep the finalizer itself: it is what guarantees an observable terminating window, which the VM path's "exclude terminating `Tag`s" rule and T026's deletion-race handling both depend on
- [ ] T019 [P] [US2] [vmop-11017] Unit tests for the controller in `controllers/vspherepolicy/tag/tag_controller_test.go` + suite bootstrap `tag_controller_suite_test.go`: finalizer lifecycle, status, label-mirror correction, delete-at-zero-owners, and that the controller performs **no** VM `List` and enqueues no VM on any path — a controller-side fan-out would now be a silent duplicate of the watch
- [ ] T020 [P] [US2] [vmop-11018] Unit test that a label-only VM receives `TagSpec{add}` and is **not** added as an owner, in `pkg/vmconfig/affinitytag/affinitytag_reconciler_test.go`
- [ ] T020a [P] [US2] [vmop-11051] Integration test (envtest) for the watch wiring, in `controllers/virtualmachine/virtualmachine/virtualmachine_controller_test.go`: with `TaggingAPI` on, creating a `Tag` that matches a label-only VM's label causes that VM to be reconciled, and deleting the `Tag` does so again. This is the only test that exercises the index registration, mapper and predicate through a real manager, so a mis-wired `Watches` or a missing `RegisterAffinityTagIndexes` call fails here rather than on a Supervisor
- [ ] T021 [P] [US2] [vmop-11019] Integration test (envtest) that each field index resolves as specified, in `controllers/vspherepolicy/tag/tag_controller_test.go`: `spec.label` returns every `Tag` for a key, `spec.labelKeyValue` returns the exact pair, `metadata.ownerReferences.uid` returns a VM's owned `Tag`s, and `metadata.labels.keyValue` returns exactly the label-carrying VMs — not same-key-different-value VMs, and not VMs in other namespaces
- [ ] T022 [P] [US2] [vmop-11020] Test the no-op fan-out property: a VM enqueued by a `Tag` change whose desired tag set already equals its ExtraConfig record emits no `TagSpec`, appends no ExtraConfig entry, issues no reconfigure, and patches neither the VM nor any `Tag` — so a fan-out cannot bump `resourceVersion` and re-trigger watchers. Unit level in `pkg/vmconfig/affinitytag/affinitytag_reconciler_test.go`, plus a `Consistently` guard at the envtest level (a `Tag` status or owner-reference write bumps no VM `resourceVersion`) in `controllers/vspherepolicy/tag/tag_controller_test.go`

## Phase 5 — User Story 3 (affinity spec changes and tagging converges)

- [ ] T023 [US3] [vmop-11021] Gate `validateImmutableVMAffinity` on `Features.TaggingAPI` in `webhooks/virtualmachine/validation/virtualmachine_validator.go`, leaving `validateVMAffinity`'s shape checks applying to the new value on update
- [ ] T024 [P] [US3] [vmop-11022] Unit tests for affinity mutability (mutable flag-on, rejected flag-off) in `webhooks/virtualmachine/validation/virtualmachine_validator_test.go`
- [ ] T025 [US3] [vmop-11023] Implement delete-at-zero-owners in the `Tag` controller (`controllers/vspherepolicy/tag/tag_controller.go`) — `Delete` and return; the resulting `deletionTimestamp` is the watch event that wakes the affected VMs, so nothing is enqueued here; the case ordinary garbage collection does not cover; see [`plan.md`](./plan.md) "Complexity tracking"
- [ ] T026 [US3] [vmop-11024] Handle the deletion/re-create race in `ReconcileTagCRs` (`pkg/vmconfig/affinitytag/affinitytag_reconciler.go`): a `Tag` found with a non-zero `deletionTimestamp` yields `pkgerr.RequeueError` — never adopted, never silently skipped (spec G12)
- [ ] T027 [P] [US3] [vmop-11025] Integration tests (envtest) in `controllers/vspherepolicy/tag/tag_controller_test.go`: real GC deletes the `Tag` when the last owning VM is deleted, does not when one of two owners is deleted, and two concurrent owner-reference writers both survive

## Phase 6 — User Story 4 (CSP admin diagnosis, and the API stays invisible to DevOps users)

- [ ] T028 [US4] [vmop-11026] Implement the validating webhook (rules V1-V6 from [`model.md`](./model.md) "Admission rules") in `webhooks/vspherepolicy/tag/validation/tag_validator.go`, with `webhooks/vspherepolicy/webhooks.go` registering it from `webhooks/webhooks.go` behind the flag
- [ ] T028a [US4] [vmop-11042] V6 MUST allow-list `system:serviceaccount:kube-system:generic-garbage-collector` and `system:serviceaccount:kube-system:namespace-controller`, checked before the `IsPrivilegedAccount` test — mirroring `webhooks/persistentvolumeclaim/validation/persistentvolumeclaim_validator.go:36-43`. Without it, GC's owner-reference-pruning PATCH is denied (so a stale owner reference blocks delete-at-zero-owners forever) and namespace teardown wedges in `Terminating`. See [`model.md`](./model.md) "V6's system-account allow-list" and D15
- [ ] T029 [P] [US4] [vmop-11027] Unit tests for every admission rule, positive and negative, including the privileged-vs-DevOps split and the derived-name check, in `webhooks/vspherepolicy/tag/validation/tag_validator_test.go` + suite bootstrap
- [ ] T029a [P] [US4] [vmop-11043] Unit tests for the V6 allow-list: each allow-listed account admitted on UPDATE and DELETE, an arbitrary other `kube-system` service account still rejected, and a DevOps user still rejected on all three operations (spec G9, US4 scenario 3)
- [ ] T030 [P] [US4] [vmop-11028] Verify the generated printer columns and status subresource behave as specified, in the envtest portion of `controllers/vspherepolicy/tag/tag_controller_test.go`

## Phase 7 — E2E

- [ ] T031 [US1] [US2] [vmop-11029] Add the E2E suite `VMAffinityTagSpec` in `test/e2e/vmservice/vmservice/virtualmachine/vm_affinity_tag.go`, registered from `test/e2e/vmservice/vmservice_test.go` under `Context("VM-AFFINITY-TAG", ...)`, covering the US1 and US2 scenario tables in [`e2e.md`](./e2e.md)
- [ ] T032 [US3] [US4] [vmop-11030] Extend that suite with the US3 (affinity mutation, `Tag` cleanup, label-only participation after an affinity change) and US4 (admission rejection for a DevOps user) scenarios from [`e2e.md`](./e2e.md)
- [ ] T033 [P] [vmop-11031] Add the wait-interval keys the suite needs to `test/e2e/vmservice/config/wcp.yaml`, any consts additions to `test/e2e/vmservice/consts/consts.go`, and — if the suite introduces a new label or config knob — the corresponding note in `test/e2e/README.md` per `e2e-testing.md`. No scheme change is needed: `test/e2e/vmservice/common/scheme.go` calls the module's `AddToScheme`, which picks `Tag` up via its `init()`

## Phase Final — Polish

- [ ] T034 [vmop-11032] Run `make generate-manifests` and check in the regenerated `config/rbac/role.yaml` and webhook manifests
- [ ] T035 [vmop-11033] Add release notes per `pull-request-standards.md`: namespace-wide affinity tagging independent of `VirtualMachineGroup`, and `spec.affinity` becoming mutable when the feature is enabled
- [ ] T036 Flip `spec.md` status to `Implemented` once every acceptance criterion is covered (the `.sdd/INDEX.md` row for 004 already exists; update its Status column in the same change)
- [ ] T037 Remove `"experimental"` from the E2E labels once the suite has been validated on a real Supervisor (per `e2e-testing.md`)

## Deferred (tracked as follow-ups, not part of this change set)

Recorded in [`research.md`](./research.md) "Open follow-ups" — each needs its own spec or story before it is picked up:

- Wire `TaggingAPI` to a Supervisor capability key and return the default to `false` (spec NG6).
- Remove `genConfigSpecTagSpecsFromVMLabels` and its call sites once the `Tag`-driven path is GA (spec NG4).
- Populate the reserved vCenter Tag UUID field on `Tag` status (spec NG1).
- Remove the `VirtualMachineGroup` dependency from affinity end to end (spec NG3).
